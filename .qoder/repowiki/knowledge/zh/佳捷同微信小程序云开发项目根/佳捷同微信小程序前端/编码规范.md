- 每个页面独立成 pages/<name>/ 目录，包含 .js/.json/.wxml/.wxss 四件套文件
- 页面间通过 wx.cloud.callFunction 调用云函数进行数据交互，不直接共享内存状态
- 全局常量（如云环境 ID）集中定义在 App.globalData 中，页面按需读取
- 图标资源统一存放在 images/icons/ 目录下，按功能命名（如 home.png、goods-active.png）