- 页面数据通过 `this.setData` 批量更新，并在 finally 块中统一重置 loading 状态以保证 UI 一致性。
- 枚举值（如 direction、status）在 WXML 模板中使用三元表达式直接映射为用户可读文本，同时 JS 层保留同名 format* 方法作为备用。
- 分页加载采用 page 自增 + pageSize 固定值（20）的方式，通过 hasMore 判断是否还有下一页数据。
- 云函数调用统一使用 `wx.cloud.callFunction({ name, data })` 结构，action 参数区分不同操作。