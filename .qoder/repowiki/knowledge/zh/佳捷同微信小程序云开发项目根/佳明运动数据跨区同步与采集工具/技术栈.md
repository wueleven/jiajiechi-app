Node.js 18+ / TypeScript 4.3 + ts-node 运行；Garmin Connect 通过 `@gooin/garmin-connect` 与 `garmin-connect` 双库访问；Google Sheets 使用 `googleapis` + `google-auth-library` JWT；SQLite 存储会话状态（`sqlite` + `sqlite3`）；Strava 通过 `strava-v3`；HTTP 请求统一用 `axios`；构建与容器化使用 yarn、Docker、docker-compose；CI 通过 GitHub Actions YAML 工作流驱动。